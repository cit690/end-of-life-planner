<%- include('../partials/html-head') %>
<link rel="stylesheet" href="/stylesheets/style.css">
<%- include('../partials/nav') %>

<section>
  <h1><%= title %> </h1>
  <% if (user) { %> 
  <ul>
    <% profiles.forEach(profile => { %> 
      <li>
        <a href="/profiles/<%= profile._id %>"><%= profile.name %>, <%= profile.location %></a>
      </li>
    <% }) %> 
  </ul>
  <% } %> 
</section>

<%- include('../partials/footer') %>